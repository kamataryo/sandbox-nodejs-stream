# SANDBOC Node.js stream API

## gzip and cipher

```shell
$ node gzip-cipher-server.js
```

```shell
$ node gzip-cipher-server-client.js
```

## Back pressure

```shell
$ node backpressure-server.js
```

```shell
$ curl http://localhost:3000
```
